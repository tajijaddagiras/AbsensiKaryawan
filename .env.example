# Database Configuration
# PostgreSQL connection string
# Get this from your database provider (Vercel Postgres, Supabase, Neon, etc.)
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# Direct URL for Prisma migrations (optional)
# Use this if your provider uses connection pooling
# For Vercel Postgres: use the non-pooled connection string
DIRECT_URL="postgresql://user:password@host:port/database?schema=public"

# Application Configuration (if any)
# Add other environment variables your app needs here
# Example:
# JWT_SECRET="your-secret-key"
# NEXTAUTH_SECRET="your-nextauth-secret"
# NEXTAUTH_URL="https://your-domain.vercel.app"
